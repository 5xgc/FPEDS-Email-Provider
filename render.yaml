services:
  - type: web
    name: fpeds-mail
    runtime: python
    rootDir: .
    buildCommand: pip install -r requirements.txt && pnpm install --frozen-lockfile && PORT=10000 BASE_PATH=/ pnpm --filter @workspace/fpeds run build && test -f dist/public/index.html
    startCommand: python -m gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    healthCheckPath: /api/healthz
    envVars:
      - key: SESSION_SECRET
        sync: false
      - key: FPEDS_MAIL_DOMAIN
        value: fpdf.2bd.net
      - key: FLASK_ENV
        value: production
      - key: SQLITE_PATH
        value: /var/data/fpeds.sqlite3
      - key: BREVO_API_KEY
        sync: false
      - key: BREVO_SENDER_DOMAIN
        value: fpeds.2bd.net
      - key: BREVO_SENDER_NAME
        value: FPEDS
      - key: MAILGUN_DOMAIN
        value: fpdf.2bd.net
      - key: MAILGUN_API_KEY
        sync: false
      - key: MAILGUN_SIGNING_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false
    disk:
      name: fpeds-mail-data
      mountPath: /var/data
      sizeGB: 1